import { HashnodeService } from "../services/hashnodeService";
import { NotionService } from "../services/notionService";

class NotionController {
    private readonly notionService: NotionService = new NotionService();
    private readonly hashnodeService: HashnodeService = new HashnodeService();
    constructor() { }

    

}